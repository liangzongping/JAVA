/**
 * 接口，实际上是一个模板（类的模板），相当于一个问题有多种做法，这些做法由不同的人（类）来实现（implements）
 * 在这里接口menu是一个有两个菜的菜单，做（实现）这两个菜有两个厨师（两个类）；
 */

public interface menu {
    void xihongshijidan();
    void hongshaoniurou();
}
